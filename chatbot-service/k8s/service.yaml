apiVersion: v1
kind: Service
metadata:
  name: chatbot-service
  namespace: chatbot
spec:
  type: ClusterIP
  selector:
    app: chatbot-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000

